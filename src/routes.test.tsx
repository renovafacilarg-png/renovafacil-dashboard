import { describe } from 'vitest';

describe.skip('Routes: auth guard', () => {
  // Wave 1 will implement: unauthenticated users redirect to /login
  // Wave 1 will implement: authenticated users see DashboardLayout
});
